# Pulse (governance layer)
PULSE_HOST=127.0.0.1
PULSE_API_PORT=8100
PULSE_MCP_PORT=8101

# Nexus (coordination layer)
NEXUS_HOST=127.0.0.1
NEXUS_PORT=8202
NEXUS_PROJECT_ROOT=./plane

# Plane (target application) — see plane/setup.sh for the full env-file
# scaffold; this repo's scripts/setup.sh runs it for you.
